# Notes

- Set **Settings → Pages → Build and deployment → Source** to **GitHub Actions**.
- The VitePress base path is set to `/templeo/` for `https://ugate.github.io/templeo/`.
- `vitepress-jsdoc` runs before each docs build and writes API pages under `docs/api/`.
- This migration replaces the old `jsdocp` publishing flow.
